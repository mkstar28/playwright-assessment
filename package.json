{
  "name": "qa-playwright-assignment",
  "version": "1.0.0",
  "description": "Playwright + TypeScript — Sauce Demo UI, Fake Store API, and Reqres API test suites",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --project=ui",
    "test:fakestore": "playwright test --project=fakestore",
    "test:reqres": "playwright test --project=reqres",
    "test:assignment-1": "playwright test --project=ui",
    "test:assignment-2": "playwright test --project=fakestore",
    "test:headed": "playwright test --project=ui --headed",
    "report": "playwright show-report"
  },
  "keywords": ["playwright", "typescript", "qa", "automation"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.0.1"
  }
}
